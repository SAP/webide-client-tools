{
	"name" : "sap.watt.common.service.ui.ConfigAvailability",
	"description" : "The config part interface. Base interface for all config components",
	"methods" : {
		"isAvailable" : {
			"description": "Returns true/false which controls the configuration pane visibility",
			"params" : [{
                "name" : "oProjectDocument",
                "type" : "object",
                "description": "Project document data"
                }],
			"returns" : {
				"type" : "boolean",
				"default" : "true"
			}
		}
	}
}