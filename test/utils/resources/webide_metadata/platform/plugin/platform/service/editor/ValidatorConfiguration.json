{
	"name": "sap.watt.common.service.editor.ValidatorConfiguration",
	"description": "Interface for validators who has configuration display",
	"methods": {
		"saveConfiguration": {
			"params": [
				{
					"name": "projectPath",
					"type": "string",
					"description": "string representing the project path"
				},
				{
					"name": "configuration",
					"type": "object",
					"description": "json representing the validator's configuration in display format"
				}
			]
		},
		"getDefaultConfiguration": {
			"params": [
				{
					"name": "projectPath",
					"type": "string",
					"description": "string representing the project path"
				}
			],
			"returns": "object",
			"description": "get validator's default display configuration in json format"
		},
		"getConfiguration": {
			"params": [
				{
					"name": "projectPath",
					"type": "string",
					"description": "string representing the project path"
				}
			],
			"returns": "object",
			"description": "get validator's display configuration in json format"
		}
	},
	"events": {}
}