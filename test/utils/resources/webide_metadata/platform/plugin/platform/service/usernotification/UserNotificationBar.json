{
	"name" : "sap.watt.common.service.ui.UserNotificationBar",
	"description" : "The usernotificationbar interface",
	"extends" : "sap.watt.common.service.ui.Part",
	"methods" : {
		"display" : {
			"params" : [
				{
					"name" : "sMessage",
					"type":"string",
					"description": "the message to display in the user notification"
				},
				{
					"name" : "sCommandId",
					"type":"string",
					"description":"the id of the command to display in the user notification as possible action"
				},
				{
					"name" : "bAutoDisappear",
					"type":"boolean",
					"description": "true if the notification should be disappeared by itself after some short time, false if the notification should appear until it is replaced by another notification"
				},
				{
					"name" : "oCommandExecuteValue",
					"type":"object",
					"description":"the value to pass to the execute method of the command (optional)"
				}
			]
		}
	}
}
