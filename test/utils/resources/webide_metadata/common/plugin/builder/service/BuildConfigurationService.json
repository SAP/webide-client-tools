{
	"name": "sap.watt.common.builder.service.BuildConfigurationService",
	"description": "The build configuration interface",
	"methods": {
		"getBuildConfigurationTitle": {
			"params": [
				{
					"name" : "sProjectPath",
					"type" :  "string",
					"optional" : true,
					"description": "Path of the project that to be edited. If not defined actual selection is used."
				}
			],
			"returns": "string",
			"description": "The build configuration title"
		},
		"getBuildConfigurationContent": {
			"params": [
				{
					"name": "id",
					"type": "string"
				},
				{
					"name": "group",
					"type": "string"
				},
				{
					"name": "sProjectPath",
					"type": "string",
					"optional": true,
					"description": "Path of the project that to be edited. If not defined actual selection is used."
				}
			],
			"returns": "object"
		},
		"saveBuildConfiguration": {
			"params": [
				{
					"name": "id",
					"type": "string"
				},
				{
					"name": "group",
					"type": "string"
				},
				{
					"name": "sProjectPath",
					"type": "string",
					"optional": true,
					"description": "Path of the project that to be edited. If not defined actual selection is used."
				}
			],
			"returns": "object"
		}
	}
}