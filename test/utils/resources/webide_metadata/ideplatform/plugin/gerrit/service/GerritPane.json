{
	"name": "sap.watt.ideplatform.plugin.gerrit.service.GerritPane",
	"extends": ["sap.watt.common.service.ui.Part"],
	"methods": {
		"setNewChanges": {
			"description": "show new Gerrit Changes",
			"params": [{
				"name": "mAllGerritChanges",
				"type": "map"
			}, {
				"name": "sSellectedProject",
				"type": "string"
			}]
		},
		"refreshGerritChanges": {
			"description": "refresh Gerrit Changes"
		},
		"isNotGerritProject": {
			"description": "set annotation in gerrit pane when the selected project is not use Gerrit"
		}
	}
}