{
	"name": "sap.watt.ideplatform.runtimeextensions.RunWithFrame",
	"methods": {
		"getRunWithFrameUrl" : {
			"description" : "Returns the URL of the application to run it with a frame.",
			"params" : [
				{
					"name" : "oAppUrl",
					"type" : "object",
					"description" : "URI of the application to run it without a frame"
				}
			],
			"returns" : {
				"type": "object",
				"description": "Application URI to run it with a frame"
			},
			"visibility" : "private"
		}
	},
	"configurationProperties": {
	}
}