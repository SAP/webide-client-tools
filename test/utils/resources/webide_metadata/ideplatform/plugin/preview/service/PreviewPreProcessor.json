{
	"name" : "sap.watt.common.plugin.preview.service.PreviewPreProcessor",
	"description" : "A preview pre-processor which will be applied before the runner is executed, e.g. inject some script into the html file",
	"methods" : {
		"apply" : {
			"params" : [{
				"name" : "oDocument",
				"type" : "object"
			}]
		}
	}
}