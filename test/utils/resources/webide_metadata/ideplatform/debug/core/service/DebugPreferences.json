{
  "name": "sap.watt.ideplatform.debug.core.service.DebugPreferences",
  "description": "Interface for debug preferences",
  "methods": {
    "getDebugClientSettings": {
      "description": "Get the debug client preference settings",
      "returns": {
        "type": "object",
        "description": "The debug client preference settings"
      }
    },
    "setDebugClientSettings": {
      "description": "Saves the debug client preference settings",
      "params": [
        {
          "name": "settings",
          "type": "object",
          "description": "The debug client preference settings"
        }
      ]
    },
    "getBreakpointManagerSettings": {
      "description": "Get the breakpoint manager settings",
      "returns": {
        "type": "object",
        "description": "The breakpoint manager preference settings"
      }
    },
    "setBreakpointManagerSettings": {
      "description": "Set or overwrite a preference node",
      "params": [
        {
          "name": "settings",
          "type": "object",
          "description": "The breakpoint manager preference settings"
        }
      ]
    }
  }
}
