{
	"name" : "sap.watt.uitools.plugin.fioriexttemplate.services.WorkspaceApplicationFactory",
	
	"description" : "Service for ABAP deployment. Builds an Application object which contains an array of RemoteDocument objects.",
	
	"methods" : {
		"getApplicationFromWorkspace": {
			"params" :  [{
			    "name" : "workspaceProjectdocument", 
			    "type": "object",
			    "description": "The document object of the project from the local workspace"
			}],
			"returns" : {
			    "type": "object",
			    "description": "Application object which contains an array of RemoteDocument objects"
			}
		}
	}
}